
### installation

1. Run `docker compose up airflow-init`
2. Run `docker compose up`
3. Run `docker compose down --volumes`

to login, use airflow/airflow

### Writing dags
1. Write your dags and put them in the dags folder

